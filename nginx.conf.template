server {
  listen ${PORT};
  server_name _;

  root /usr/share/nginx/html;

  location = /health {
    add_header Content-Type text/plain;
    return 200 "ok";
  }

  location = / {
    return 302 /ordio/;
  }

  location = /one-page-demo {
    return 301 /ordio/;
  }

  location /one-page-demo/ {
    return 301 /ordio/;
  }

  location /ordio/ {
    try_files $uri $uri/ /ordio/index.html;
  }

  location ~* ^/ordio/.*\.(css|js|png|jpg|jpeg|gif|svg|webp|ico|woff|woff2)$ {
    expires 1h;
    add_header Cache-Control "public, max-age=3600";
    try_files $uri =404;
  }
}
